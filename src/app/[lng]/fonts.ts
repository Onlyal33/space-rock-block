import { Open_Sans, Passion_One } from 'next/font/google';

export const openSans = Open_Sans({ subsets: ['latin', 'cyrillic'] });
export const passionOne = Passion_One({ subsets: ['latin'], weight: '400' });
